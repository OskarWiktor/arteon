import LocaleLayout from '@/components/shared/LocaleLayout';

export default function HaLayout({ children }: { children: React.ReactNode }) {
  return <LocaleLayout locale="ha">{children}</LocaleLayout>;
}
