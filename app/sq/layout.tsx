import LocaleLayout from '@/components/shared/LocaleLayout';

export default function SqLayout({ children }: { children: React.ReactNode }) {
  return <LocaleLayout locale="sq">{children}</LocaleLayout>;
}
