import LocaleLayout from '@/components/shared/LocaleLayout';

export default function IgLayout({ children }: { children: React.ReactNode }) {
  return <LocaleLayout locale="ig">{children}</LocaleLayout>;
}
